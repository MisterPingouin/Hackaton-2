{% extends 'base.html.twig' %}

{% block body %}
	<main class="main-page" aria-label="login">
		<div class="text-center text-blue mt-5">
			<h1>Connectez-vous</h1>
		</div>
		<div class="d-flex justify-content-center">
			<div class="col-9 col-md-6">
				<div class="card shadow-lg p-2 mt-4">
					{% if error %}
						<div class="alert alert-danger text-center">
							{{ error.messageKey|trans(error.messageData, 'security') }}
						</div>
					{% endif %}

					<form action="{{ path('login') }}" method="post">
						<label class="form-label" for="username">Email:</label>
						<input class="form-control" type="text" id="username" name="_username" value="{{ last_username }}">

						<label class="form-label" for="password">Password:</label>
						<input class="form-control" type="password" id="password" name="_password">

						<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

						<div class="d-flex justify-content-center">
							<button type="submit" class="btn btn-primary my-3 text-white">Connection</button>
						</div>
						<div class="d-flex justify-content-center">
							<a href="{{ path('register') }}" class="nav-link link-primary link-offset-2 link-underline-opacity-25 link-underline-opacity-100-hover">Vous n'avez pas encore de compte ?</a>
						</div>
					</form>
				</div>
			</div>
		</div>
	</main>
{% endblock %}
