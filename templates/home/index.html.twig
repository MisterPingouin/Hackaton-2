{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block body %}
    
			<div class="col-xl-6">

				{{ form_start(form) }}		
       				<div>
      				      {{ form_row(form.marque) }}
       				</div>

        			<div>
          				{{ form_row(form.modele) }}
        			</div>

					<div class="text-dark">
						{{form_row(form.ram)}}
					</div>
					
					<div class="form-tag text-dark">
						{{ form_row(form.stockage) }}
					</div>

				
				<button class="btn btn-light">{{ button|default('Envoyer') }}</button>
				{{ form_end(form) }}
			</div>
{% endblock %}
